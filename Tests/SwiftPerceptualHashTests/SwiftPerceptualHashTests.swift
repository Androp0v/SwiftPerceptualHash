import XCTest
@testable import SwiftPerceptualHash

final class SwiftPerceptualHashTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftPerceptualHash().text, "Hello, World!")
    }
}
